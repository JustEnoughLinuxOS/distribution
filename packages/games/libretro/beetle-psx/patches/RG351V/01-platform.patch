diff -rupN beetle-psx-libretro.orig/Makefile beetle-psx-libretro.rg351x/Makefile
--- beetle-psx-libretro.orig/Makefile   2022-05-10 13:48:03.460683618 -0700
+++ beetle-psx-libretro.rg351x/Makefile 2022-05-10 19:03:01.578600164 -0700
@@ -346,6 +346,21 @@ else ifeq ($(platform), rpi4_64)
    GL_LIB := -lGLESv2
    HAVE_CDROM = 0

+# Anbernic RG351x / RK3326
+else ifeq ($(platform), rg351x)
+   TARGET := $(TARGET_NAME)_libretro.so
+   fpic   := -fPIC
+   GREP = grep
+   SHARED := -shared -Wl,--no-undefined -Wl,--version-script=link.T
+   CFLAGS   += -march=armv8-a+crc -mcpu=cortex-a35 -mtune=cortex-a35
+   CXXFLAGS += -march=armv8-a+crc -mcpu=cortex-a35 -mtune=cortex-a35
+   LDFLAGS += $(PTHREAD_FLAGS) -ldl -lrt
+   HAVE_LIGHTREC = 1
+   FLAGS += -DHAVE_SHM
+   GLES = 1
+   GL_LIB := -lGLESv2
+   HAVE_CDROM = 0
+
 # Windows MSVC 2017 all architectures
 else ifneq (,$(findstring windows_msvc2017,$(platform)))

